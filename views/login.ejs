<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Connexion - Cinéphoria</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Google Fonts & Bootstrap & FontAwesome -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500&family=Cinzel:wght@600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">

  <style>
    body { min-height:100vh; display:flex; flex-direction:column;
      background: radial-gradient(circle at 20% 30%, #1a1f36, #0a0f1f);
      background-size:300% 300%; animation: gradientMove 15s ease infinite;
      color:#f1f1f1; font-family:'Poppins',sans-serif; opacity:0; transition:opacity 1.2s ease; }
    body.loaded { opacity:1; }
    @keyframes gradientMove { 0%{background-position:0% 50%} 50%{background-position:100% 50%} 100%{background-position:0% 50%} }
    .cinema-card { background:rgba(255,255,255,0.07); border:1.2px solid rgba(245,197,24,0.33);
      border-radius:18px; backdrop-filter:blur(15px);
      box-shadow:0 0 38px rgba(0,0,0,0.75); transition:transform .4s, box-shadow .4s; }
    .cinema-card:hover { transform: translateY(-4px) scale(1.013); box-shadow:0 0 50px rgba(245,197,24,0.23); }
    .fade-in { opacity:0; transform:translateY(25px); animation: fadeInUp 1s ease forwards; }
    @keyframes fadeInUp { to { opacity:1; transform:translateY(0); } }
    h1 { font-family:'Cinzel',serif; color:#f5c518; text-shadow:0 0 14px rgba(245,197,24,0.5),0 0 18px rgba(0,0,0,0.8);
      font-size:2.1rem; margin-bottom:1.2em; }
    .cinema-label { color:#f5c518; font-weight:500; text-shadow:0 0 6px rgba(0,0,0,0.7); }
    .form-control { background:rgba(255,255,255,0.10); border:1px solid rgba(255,255,255,0.18); color:#fff; border-radius:8px; }
    .form-control:focus { background:rgba(0,0,0,0.3); border-color:#f5c518; box-shadow:0 0 10px rgba(245,197,24,0.48); color:#fff; }
    .input-group-text { background:rgba(245,197,24,0.12); border:1px solid rgba(245,197,24,0.19); color:#f5c518; }
    .invalid-feedback{ color:#ff4257; } .valid-feedback{ color:#32d677; }
    .btn-cinema { background:linear-gradient(45deg,#ff004d,#a700ff); color:#fff; font-weight:bold; border-radius:8px; padding:13px; border:none;
      text-shadow:0 0 6px rgba(0,0,0,0.8); transition:transform .4s, box-shadow .4s; position:relative; overflow:hidden; margin-top:15px; }
    .btn-cinema:hover { transform:scale(1.05); box-shadow:0 0 24px rgba(245,197,24,0.46); }
    .small-register { font-size:1.01rem; color:#f5c518; text-decoration:underline; text-underline-offset:3px; }
    .small-register:hover { color:#ffd966; }
    @media (max-width:540px){ .cinema-card{ padding:1.2rem .7rem 1.5rem } h1{ font-size:1.15rem } }
    /* Forcer visibilité checkbox + label */
.form-check-label {
  color: #ffffff !important;
  opacity: 1 !important;
  font-weight: 500;
}

/* Forcer visibilité checkbox */
.form-check-input {
  border-color: #f5c518;
  background-color: rgba(0,0,0,0.7);
}

.form-check-input:checked {
  background-color: #f5c518;
  border-color: #f5c518;
}

  </style>
</head>
<body>
  <%- include('partials/header') %>

  <main class="container mt-5 mb-5 fade-in">
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <div class="card cinema-card shadow p-5">
          <h1 class="text-center">🎬 Connexion à Cinéphoria</h1>

          <% if (errorMessage) { %>
            <div class="alert alert-danger text-center" role="alert" aria-live="polite"><%= errorMessage %></div>
          <% } %>
            <!-- Message de succès -->
<% if (success && success.length > 0) { %>
  <div class="alert alert-success text-center" role="alert">
    <%= success[0] %>
  </div>
<% } %>

<!-- Message d’erreur (flash, différent de errorMessage) -->
<% if (error && error.length > 0) { %>
  <div class="alert alert-danger text-center" role="alert">
    <%= error[0] %>
  </div>
<% } %>

          <form action="/login" method="POST" autocomplete="on" novalidate id="loginForm">
            <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <% } %>

            <div class="mb-3">
              <label for="email" class="form-label cinema-label">Adresse e-mail</label>
              <div class="input-group">
                <span class="input-group-text" id="emailIcon"><i class="fa fa-envelope"></i></span>
                <input
                  type="email" class="form-control" id="email" name="email"
                  required autocomplete="email" aria-describedby="emailIcon"
                  value="<%= typeof email !== 'undefined' ? email : '' %>">
              </div>
              <div class="invalid-feedback">Veuillez saisir une adresse e-mail valide.</div>
            </div>

            <div class="mb-2">
              <label for="password" class="form-label cinema-label">Mot de passe</label>
              <div class="input-group">
                <span class="input-group-text" id="pwdIcon"><i class="fa fa-lock"></i></span>
                <input type="password" class="form-control" id="password" name="password"
                       required autocomplete="current-password" aria-describedby="pwdIcon">
                <button class="input-group-text" id="togglePwd" type="button" aria-label="Afficher/masquer le mot de passe">
                  <i class="fa fa-eye" id="iconPwdEye"></i>
                </button>
              </div>
              <div class="invalid-feedback">Veuillez entrer votre mot de passe.</div>
            </div>

            <div class="d-flex justify-content-between align-items-center mt-1">
              <div class="form-check">
                <input class="form-check-input" type="checkbox" id="remember" name="remember">
                <label class="form-check-label" for="remember">Se souvenir de moi</label>
              </div>
<a href="/forgot-password" class="small-register">Mot de passe oublié ?</a>

            </div>

            <div class="d-grid mt-3">
              <% if (typeof redirect !== 'undefined' && redirect) { %>
                <input type="hidden" name="redirect" value="<%= redirect %>">
              <% } %>
              <button id="submitBtn" type="submit" class="btn btn-cinema btn-lg">
                Se connecter
              </button>
            </div>

            <div class="text-center mt-4">
              <span class="text-light">Pas encore de compte ?</span>
              <a href="/register" class="small-register">Inscrivez-vous</a>
            </div>
          </form>

        </div>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // transition de fond
    window.addEventListener("load", () => { document.body.classList.add("loaded"); });

    // Afficher/masquer mot de passe (robuste)
    (function () {
      const inputPwd = document.getElementById('password');
      const toggleBtn = document.getElementById('togglePwd');
      const eyeIcon  = document.getElementById('iconPwdEye');
      if (inputPwd && toggleBtn && eyeIcon) {
        toggleBtn.addEventListener('click', () => {
          const txt = inputPwd.type === 'password';
          inputPwd.type = txt ? 'text' : 'password';
          eyeIcon.classList.toggle('fa-eye');
          eyeIcon.classList.toggle('fa-eye-slash');
        });
      }
    })();

    // Validation client (Bootstrap) + désactivation bouton pendant envoi
    (function () {
      'use strict';
      const form = document.getElementById('loginForm');
      const submitBtn = document.getElementById('submitBtn');

      if (!form) return;
      form.addEventListener('submit', function (event) {
        if (!form.checkValidity()) {
          event.preventDefault(); event.stopPropagation();
        } else {
          // lock UI pendant la soumission
          if (submitBtn) {
            submitBtn.disabled = true;
            submitBtn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span> Connexion...';
          }
        }
        form.classList.add('was-validated');
      }, false);
    })();
  </script>
</body>
</html>
