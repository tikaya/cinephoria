<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Inscription réussie - Cinéphoria</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
  <style>
    /* Conteneur premium pour le message de succès */
    .cinema-success {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(245,197,24,0.3);
      border-radius: 15px;
      backdrop-filter: blur(12px);
      box-shadow: 0 0 25px rgba(0,0,0,0.7);
      padding: 30px;
      animation: popIn 0.6s ease-in-out;
    }
    @keyframes popIn {
      0% { transform: scale(0.9); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }
    .cinema-success p {
      color: #e4e4e4;
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <main class="container mt-5 mb-5">
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <div class="cinema-success text-center shadow">
          <h1 class="mb-4 text-cinema">🎉 Bienvenue sur Cinéphoria !</h1>
          <p class="lead">Votre compte a été <strong class="text-success">créé avec succès</strong>.</p>
          <hr class="border-secondary">
          <p>Connectez-vous dès maintenant pour découvrir nos films et réserver vos séances.</p>
          <a href="/login" class="btn btn-cinema mt-4">Se connecter</a>
        </div>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Effet de fondu du body
    window.addEventListener("load", () => {
      document.body.classList.add("loaded");
    });
    setTimeout(() => {
      document.body.classList.add("loaded");
    }, 1000);
  </script>
</body>
</html>
