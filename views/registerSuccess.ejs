<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Inscription réussie - Cinéphoria</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
  <style>
    .netflix-success {
      background-color: #1f1f1f;
      border: 2px solid #e50914;
      border-radius: 8px;
      padding: 30px;
      animation: popIn 0.6s ease-in-out;
    }
    @keyframes popIn {
      0% { transform: scale(0.9); opacity: 0; }
      100% { transform: scale(1); opacity: 1; }
    }
    .netflix-success p {
      color: #e5e5e5;
    }
    .btn-netflix:hover {
      transform: scale(1.05);
      box-shadow: 0 0 10px rgba(229,9,20,0.6);
    }
  </style>
</head>
<body class="bg-dark text-light"> <!-- Pas de fade-start -->
  <%- include('partials/header') %>

  <main class="container mt-5 mb-5">
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <div class="netflix-success text-center shadow">
          <h1 class="mb-4 text-netflix">🎉 Bienvenue sur Cinéphoria !</h1>
          <p class="lead">Votre compte a été <strong class="text-success">créé avec succès</strong>.</p>
          <hr class="border-secondary">
          <p>Connectez-vous dès maintenant pour découvrir nos films et réserver vos séances.</p>
          <a href="/login" class="btn btn-netflix mt-4">Se connecter</a>
        </div>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
  // Rendre le body visible après le chargement
  window.addEventListener("load", () => {
    document.body.classList.add("loaded");
  });

  // Si JS plante ou prend trop de temps, forcer l'affichage après 1s
  setTimeout(() => {
    document.body.classList.add("loaded");
  }, 1000);
</script>

</body>
</html>
