<nav class="navbar navbar-expand-lg cinema-navbar shadow-custom">
  <div class="container-fluid">
    <!-- LOGO -->
    <a class="navbar-brand cinema-logo fw-bold px-3 d-flex align-items-center" href="/employe/home"
       style="font-family:'Cinzel',serif; color:#f5c518; font-size:2.1rem; letter-spacing:1px; text-shadow:0 1px 18px rgba(245,197,24,0.40),0 1px 2px #000;">
      <i class="fas fa-film me-2" style="font-size:1.6rem;"></i> Cinéphoria
      <span class="ms-2 badge bg-warning text-dark rounded-pill" style="font-size:.75rem;">Intranet</span>
    </a>

    <!-- Toggler mobile -->
    <button class="navbar-toggler border-0 bg-transparent" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-label="Menu">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <!-- Onglets INTRANET UNIQUEMENT -->
      <ul class="navbar-nav ms-auto gap-1 align-items-lg-center">
        <li class="nav-item">
          <a class="nav-link nav-anim text-light px-3" href="/employe/home">
            <i class="fa-solid fa-house me-1"></i> Accueil
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link nav-anim text-light px-3" href="/intranet/films">
            <i class="fas fa-film me-1"></i> Films
          </a>
        </li>

        <li class="nav-item">
         <a class="nav-link nav-anim text-light px-3" href="/intranet/seances/hub">
          <i class="fa-solid fa-clapperboard me-1"></i> Séances
        </a>

        </li>

        <li class="nav-item">
          <a class="nav-link nav-anim text-light px-3" href="/intranet/get/salleHub">
            <i class="fa-solid fa-vector-square me-1"></i> Salles
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link nav-anim text-light px-3" href="/intranet/avis">
            <i class="fa-solid fa-comments me-1"></i> Avis
          </a>
        </li>

        <!-- Profil + Logout -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-light" href="#" role="button" data-bs-toggle="dropdown">
            <i class="fa-solid fa-user-tie me-1"></i>
            <%= currentUser?.email || "Employé" %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end bg-dark border-0 shadow">
            <li class="px-3 py-2 text-light-50 small">
              Connecté en tant que<br><strong class="text-warning"><%= currentUser?.email || "Employé" %></strong>
            </li>
            <li><hr class="dropdown-divider border-warning"></li>
            <li class="px-3">
              <form action="/logout" method="GET" class="m-0 p-0">
                <% if (typeof csrfToken !== 'undefined' && csrfToken) { %>
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <% } %>
                <button type="submit" class="dropdown-item text-danger w-100">
                  <i class="fa-solid fa-right-from-bracket me-1"></i> Se déconnecter
                </button>
              </form>
            </li>
          </ul>
        </li>
      </ul>
      <!-- FIN onglets Intranet -->
    </div>
  </div>
</nav>

<style>
.cinema-navbar {
  background: rgba(12, 18, 38, 0.75);
  backdrop-filter: blur(13px) saturate(165%);
  border-bottom: 1.2px solid rgba(245,197,24,0.22);
  box-shadow: 0 2px 30px 0 rgba(245,197,24,0.07), 0 1.5px 0 rgba(255,255,255,0.10);
  z-index: 1001;
}
.shadow-custom {
  box-shadow: 0 5px 32px -2px rgba(245,197,24,0.15), 0 1.5px 0 rgba(0,0,0,0.09) !important;
}
.cinema-logo {
  letter-spacing: 1.5px;
  text-shadow: 0 0 13px rgba(245,197,24,0.14), 0 1.5px 0 #000;
  transition: color 0.18s, text-shadow 0.23s;
}
.navbar-nav .nav-link {
  position: relative;
  font-size: 1.10rem;
  font-weight: 500;
  border-radius: 19px;
  transition: color 0.22s, background 0.17s, box-shadow 0.23s;
}
.nav-anim::before {
  content: '';
  position: absolute;
  left: 14%; right: 14%; bottom: 8px;
  height: 2.4px;
  background: linear-gradient(90deg, transparent, #f5c518 75%, transparent);
  border-radius: 2px;
  opacity: 0; transition: opacity 0.24s, width 0.22s; width: 72%;
}
.nav-anim:hover, .nav-anim:focus {
  color: #f5c518 !important;
  background: rgba(245,197,24,0.07);
  box-shadow: 0 2px 8px rgba(245,197,24,0.11);
  text-shadow: 0 0 10px #f5c51833;
}
.nav-anim:hover::before, .nav-anim:focus::before { opacity: 1; }
.navbar-toggler { color: #f5c518; border: none; }
.navbar-toggler:focus { outline: none; box-shadow: 0 0 0 2px #f5c51855; }
.dropdown-menu a:hover { background: rgba(245,197,24,0.1); }
</style>
