<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mise à jour réussie - Cinéphoria</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@600&family=Poppins:wght@400;600&display=swap" rel="stylesheet">

  <style>
    /* 🌙 Isolation du thème Cinéphoria */
    .page-update-success {
      background-color: #0a0f1f !important;
      color: #e4e4e4 !important;
      font-family: 'Poppins', sans-serif;
      overflow-x: hidden;
      min-height: 100vh;
    }

    .page-update-success h1.text-cinema {
      font-family: 'Cinzel', serif;
      color: #f5c518 !important;
      text-shadow: 0 0 12px rgba(245,197,24,0.7);
      letter-spacing: 1px;
      margin-bottom: 40px;
    }

    .page-update-success .card-cinema {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(245,197,24,0.25);
      border-radius: 16px;
      backdrop-filter: blur(10px);
      box-shadow: 0 0 25px rgba(0,0,0,0.6);
      padding: 2rem;
      text-align: center;
      opacity: 0;
      transform: translateY(30px);
      transition: all 0.6s ease;
    }

    .page-update-success .card-cinema.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .page-update-success .card-cinema h2 {
      color: #f5c518;
      font-family: 'Cinzel', serif;
      margin-bottom: 1rem;
      text-shadow: 0 0 6px rgba(245,197,24,0.5);
    }

    .page-update-success .card-cinema p {
      color: #ddd;
      font-size: 1.05rem;
      margin-bottom: 0.5rem;
    }

    .page-update-success .btn-cinema {
      background-color: #f5c518;
      border: none;
      color: #000;
      font-weight: bold;
      text-transform: uppercase;
      padding: 0.8rem 2rem;
      border-radius: 10px;
      transition: all 0.3s ease;
      box-shadow: 0 0 10px rgba(245,197,24,0.3);
    }

    .page-update-success .btn-cinema:hover {
      background-color: #ffd84a;
      box-shadow: 0 0 15px rgba(245,197,24,0.6);
      transform: translateY(-2px);
    }

    .page-update-success .poster {
      width: 100%;
      max-width: 250px;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0,0,0,0.8);
      margin-bottom: 20px;
    }

    .page-update-success footer {
      margin-top: 60px;
      color: #aaa;
      font-size: 0.9rem;
      text-align: center;
    }

    /* 🌟 Responsive */
    @media (max-width: 768px) {
      .page-update-success .card-cinema {
        padding: 1.5rem;
      }
      .page-update-success .btn-cinema {
        width: 100%;
      }
    }
  </style>
</head>

<body class="page-update-success">
  <%- include('partials/header_employe') %>

  <main class="container text-center py-5">
    <h1 class="text-cinema">🎬 Mise à jour réussie !</h1>

    <% if (!film) { %>
      <p class="text-danger">⚠️ Erreur : aucun film à afficher.</p>
    <% } else { %>
      <div class="row justify-content-center">
        <div class="col-md-6">
          <div class="card-cinema" id="successCard">
            <% if (film.affiche_url) { %>
              <img src="<%= film.affiche_url %>" alt="<%= film.titre %>" class="poster mb-3">
            <% } %>

            <h2><%= film.titre %></h2>
            <p><strong>Genre :</strong> <%= film.genre %></p>
            <p><strong>Description :</strong> <%= film.description %></p>
            <p><strong>Âge minimum :</strong> <%= film.age_minimum %> ans</p>
            <p>
              <strong>Coup de cœur :</strong>
              <% if (film.label_coup_de_coeur) { %>
                <span style="color:#f5c518;">★ Oui</span>
              <% } else { %>
                Non
              <% } %>
            </p>

            <div class="mt-4">
              <a href="/intranet/getAllFilms" class="btn btn-cinema">⬅ Retour à la liste des films</a>
            </div>
          </div>
        </div>
      </div>
    <% } %>
  </main>

  <footer>
    <%- include('partials/footer') %>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    window.addEventListener("load", () => {
      const card = document.getElementById("successCard");
      if (card) card.classList.add("visible");
    });
  </script>
</body>
</html>
