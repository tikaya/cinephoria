<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Tous les Films - Cinéphoria</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
      background-color: #0a0f1f;
      color: #e4e4e4;
      font-family: 'Poppins', sans-serif;
      overflow-x: hidden;
    }

    h1.text-cinema {
      font-family: 'Cinzel', serif;
      color: #f5c518;
      text-shadow: 0 0 10px rgba(245,197,24,0.6);
      letter-spacing: 1px;
    }

    /* Apparition fluide des cartes */
    .film-card {
      opacity: 0;
      transform: translateY(25px);
      transition: opacity 0.6s ease, transform 0.6s ease;
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(245,197,24,0.2);
      border-radius: 12px;
      backdrop-filter: blur(10px);
      box-shadow: 0 0 20px rgba(0,0,0,0.6);
      overflow: hidden;
    }

    .film-card.visible {
      opacity: 1;
      transform: translateY(0);
    }

    /* ✅ Taille uniforme et équilibrée des affiches */
    .film-card img {
      border-top-left-radius: 12px;
      border-top-right-radius: 12px;
      height: 280px;          /* Hauteur plus discrète */
      width: 100%;            /* S’adapte à la largeur du conteneur */
      object-fit: cover;      /* Recadre sans déformation */
      transition: transform 0.4s ease, filter 0.4s ease;
      filter: brightness(90%);
    }

    .film-card img:hover {
      transform: scale(1.05);
      filter: brightness(100%);
    }

    .film-card .card-body {
      background: transparent;
      padding: 1.2rem;
    }

    .film-card h5 {
      color: #f5c518;
      font-family: 'Cinzel', serif;
      text-shadow: 0 0 6px rgba(245,197,24,0.5);
      font-size: 1.1rem;
    }

    .film-card p {
      color: #e4e4e4;
      font-size: 0.95rem;
    }

    .film-card small {
      color: #bbb;
    }

    .btn-cinema {
      background-color: #f5c518;
      border: none;
      color: #000;
      font-weight: bold;
      text-transform: uppercase;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    .btn-cinema:hover {
      background-color: #ffd84a;
      transform: translateY(-2px);
      box-shadow: 0 0 10px rgba(245,197,24,0.5);
    }

    footer {
      margin-top: 80px;
    }

    .no-film {
      color: #aaa;
      font-size: 1.2rem;
      margin-top: 50px;
      font-style: italic;
    }

    /* Harmonisation de la grille Bootstrap */
    @media (min-width: 992px) {
      .film-card {
        min-height: 480px; /* mêmes proportions visuelles entre cartes */
      }
    }
  </style>
</head>

<body>
  <header>
    <%- include('partials/header_employe') %>
  </header>

  <main class="container py-5">
    <h1 class="text-center text-cinema mb-5">🎬 Liste des Films</h1>

    <% if (films && films.length > 0) { %>
      <div class="row g-4">
        <% films.forEach((film, index) => { %>
          <div class="col-sm-6 col-md-4">
            <div class="card film-card h-100">
              <img src="<%= film.affiche_url %>" alt="<%= film.titre %>">
              <div class="card-body text-center">
                <h5 class="card-title"><%= film.titre %></h5>
                <p class="card-text"><%= film.genre %></p>
                <p><small>Âge minimum : <%= film.age_minimum %> ans</small></p>
                <div class="d-grid">
                  <a href="/intranet/films/edit/<%= film.id_film %>" class="btn btn-cinema">Modifier</a>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p class="text-center no-film">Aucun film trouvé dans la base de données.</p>
    <% } %>
  </main>

  <footer>
    <%- include('partials/footer') %>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Animation d'apparition fluide des cartes
    document.addEventListener("DOMContentLoaded", () => {
      const cards = document.querySelectorAll(".film-card");
      cards.forEach((card, i) => {
        setTimeout(() => card.classList.add("visible"), i * 150);
      });
    });
    // Effet de fondu au chargement
    window.addEventListener("load", () => {
      document.body.classList.add("loaded");
    });
  </script>
</body>
</html>
