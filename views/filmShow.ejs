<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title><%= title %> - Cinéphoria</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
  <link href="/css/styles.css" rel="stylesheet" />
  <style>
    :root { --gold:#f5c518; --bg-2:rgba(12,18,38,.72); --stroke:rgba(245,197,24,.18); --text:#eaeaea; --blur:12px; --radius:18px; }
    body { background: radial-gradient(1100px 680px at 20% -10%, #121a35 0%, #0a0f1f 45%, #05070f 100%); color:var(--text); }
    .admin-main{ padding-top:96px; padding-bottom:80px; }
    .glass{ background: var(--bg-2); border:1px solid var(--stroke); border-radius: var(--radius); backdrop-filter: blur(var(--blur)); }
    .admin-badge { background: rgba(245,197,24,.14); color:var(--gold); border:1px solid rgba(245,197,24,.35); }
    .preview { max-width: 100%; max-height: 420px; border-radius: 12px; border:1px solid var(--stroke); }
  </style>
</head>
<body>
  <%- include('partials/header') %>
  <main class="container admin-main">
    <div class="glass p-3 d-flex justify-content-between align-items-center mb-3">
      <div class="d-flex align-items-center gap-2">
        <i class="fa-solid fa-film me-2" style="color:var(--gold)"></i>
        <h1 class="h5 m-0"><%= film.titre %></h1>
        <span class="badge admin-badge">ID #<%= film.id_film %></span>
      </div>
      <div class="d-flex gap-2">
        <a class="btn btn-outline-light btn-sm" href="/admin/films"><i class="fa-solid fa-table me-1"></i> Liste</a>
        <a class="btn btn-outline-light btn-sm" href="/admin/films/<%= film.id_film %>/edit"><i class="fa-solid fa-pen-to-square me-1"></i> Modifier</a>
      </div>
    </div>

    <div class="row g-3">
      <div class="col-12 col-md-8">
        <div class="glass p-3">
          <h2 class="h6">Informations</h2>
          <dl class="row mb-0">
            <dt class="col-sm-3">Genre</dt>
            <dd class="col-sm-9"><%= film.genre ?? '—' %></dd>

            <dt class="col-sm-3">Âge minimum</dt>
            <dd class="col-sm-9"><%= film.age_minimum ?? '—' %></dd>

            <dt class="col-sm-3">Coup de cœur</dt>
            <dd class="col-sm-9"><%= film.label_coup_de_coeur ? 'Oui' : 'Non' %></dd>

            <dt class="col-sm-3">Description</dt>
            <dd class="col-sm-9"><%= film.description ?? '—' %></dd>

            <dt class="col-sm-3">Ajouté le</dt>
            <dd class="col-sm-9"><%= film.date_ajout ? new Date(film.date_ajout).toLocaleDateString('fr-FR') : '—' %></dd>
          </dl>
        </div>
      </div>
      <div class="col-12 col-md-4">
        <div class="glass p-2 text-center">
          <% if (film.affiche_url) { %>
            <img src="<%= film.affiche_url %>" alt="Affiche" class="preview" />
          <% } else { %>
            <div class="preview d-flex align-items-center justify-content-center" style="height:420px;">Aucune affiche</div>
          <% } %>
        </div>
      </div>
    </div>
  </main>
  <%- include('partials/footer') %>
<script>
    window.addEventListener("load", () => { document.body.classList.add("loaded"); });

</script>
</body>
</html>
