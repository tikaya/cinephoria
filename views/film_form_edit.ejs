<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Modifier le Film - Cinéphoria</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    body {
      background-color: #0a0f1f;
      color: #e4e4e4;
      font-family: 'Poppins', sans-serif;
      overflow-x: hidden;
    }

    h1.text-cinema {
      font-family: 'Cinzel', serif;
      color: #f5c518;
      text-shadow: 0 0 10px rgba(245,197,24,0.6);
      letter-spacing: 1px;
    }

    /* Carte formulaire translucide */
    .film-form {
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(245,197,24,0.2);
      border-radius: 15px;
      padding: 40px;
      box-shadow: 0 0 25px rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(10px);
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 0.6s ease, transform 0.6s ease;
    }

    .film-form.visible {
      opacity: 1;
      transform: translateY(0);
    }

    label {
      font-weight: bold;
      color: #f5c518;
      font-family: 'Cinzel', serif;
      letter-spacing: 0.5px;
    }

    input, textarea, select {
      background: rgba(255,255,255,0.1);
      border: 1px solid rgba(245,197,24,0.2);
      color: #fff;
      border-radius: 8px;
    }

    input:focus, textarea:focus, select:focus {
      border-color: #f5c518;
      box-shadow: 0 0 6px rgba(245,197,24,0.5);
      background: rgba(255,255,255,0.15);
    }

    .btn-cinema {
      background-color: #f5c518;
      border: none;
      color: #000;
      font-weight: bold;
      text-transform: uppercase;
      transition: background 0.3s ease, transform 0.2s ease;
    }
    .btn-cinema:hover {
      background-color: #ffd84a;
      transform: translateY(-2px);
      box-shadow: 0 0 10px rgba(245,197,24,0.5);
    }

    .btn-back {
      background: transparent;
      border: 1px solid #f5c518;
      color: #f5c518;
      transition: 0.3s ease;
    }
    .btn-back:hover {
      background-color: #f5c518;
      color: #000;
    }

    footer {
      margin-top: 80px;
    }
  </style>
</head>
<body>
  <header>
    <%- include('partials/header_employe') %>
  </header>

  <main class="container py-5">
    <h1 class="text-center text-cinema mb-5">🎬 Modifier le Film</h1>

    <form class="film-form mx-auto col-md-8" method="POST" action="/intranet/films/update/<%= film.id_film %>">
        <input type="hidden" name="_csrf" value="<%= csrfToken || '' %>">

      <div class="mb-3">
        <label for="titre" class="form-label">Titre</label>
        <input type="text" id="titre" name="titre" class="form-control" value="<%= film.titre %>" required>
      </div>

      <div class="mb-3">
        <label for="genre" class="form-label">Genre</label>
        <input type="text" id="genre" name="genre" class="form-control" value="<%= film.genre %>" required>
      </div>

      <div class="mb-3">
        <label for="age_minimum" class="form-label">Âge minimum</label>
        <input type="number" id="age_minimum" name="age_minimum" class="form-control" value="<%= film.age_minimum %>" min="0">
      </div>

      <div class="mb-3">
        <label for="description" class="form-label">Description</label>
        <textarea id="description" name="description" class="form-control" rows="4"><%= film.description %></textarea>
      </div>

      <div class="mb-3">
        <label for="affiche_url" class="form-label">URL de l’affiche</label>
        <input type="url" id="affiche_url" name="affiche_url" class="form-control" value="<%= film.affiche_url %>">
      </div>

      <div class="form-check form-switch mb-4">
        <input class="form-check-input" type="checkbox" id="label_coup_de_coeur" name="label_coup_de_coeur" value="true" <%= film.label_coup_de_coeur ? 'checked' : '' %>>
        <label class="form-check-label" for="label_coup_de_coeur">💛 Film coup de cœur</label>
      </div>

      <div class="d-flex justify-content-between mt-4">
        <a href="/intranet/films" class="btn btn-back">⬅ Retour</a>
        <button type="submit" class="btn btn-cinema">💾 Enregistrer</button>
      </div>
    </form>
  </main>

  <footer>
    <%- include('partials/footer') %>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Effet d’apparition fluide du formulaire
    document.addEventListener("DOMContentLoaded", () => {
      document.querySelector(".film-form")?.classList.add("visible");
    });
        // Effet de fondu au chargement
    window.addEventListener("load", () => {
      document.body.classList.add("loaded");
    });
    
  </script>
</body>
</html>
