<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Contact - Cinéphoria</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Fonts / Icons / Bootstrap -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500&family=Cinzel:wght@600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">

  <style>
    body {
      min-height:100vh;
      background: radial-gradient(circle at 20% 30%, #1a1f36, #0a0f1f);
      background-size:300% 300%;
      animation: gradientMove 15s ease infinite;
      color:#f2f2f2;
      font-family:'Poppins',sans-serif;
      opacity:0;
      transition:opacity 1.2s ease;
    }

    body.loaded { opacity:1; }

    @keyframes gradientMove {
      0%{background-position:0% 50%}
      50%{background-position:100% 50%}
      100%{background-position:0% 50%}
    }

    .cinema-card {
      background:rgba(255,255,255,0.08);
      border:1.5px solid rgba(245,197,24,0.35);
      border-radius:20px;
      backdrop-filter:blur(15px);
      box-shadow:0 0 35px rgba(0,0,0,0.7);
      transition:.4s;
    }

    .cinema-card:hover {
      transform:scale(1.01);
      box-shadow:0 0 60px rgba(245,197,24,0.3);
    }

    h1 {
      font-family:'Cinzel',serif;
      color:#f5c518;
      text-align:center;
      margin-bottom:1.5rem;
      text-shadow:0 0 15px rgba(245,197,24,0.5);
    }

    .cinema-label {
      color:#f5c518;
      font-weight:500;
    }

    .form-control {
      background:rgba(0,0,0,0.6);
      border:1px solid rgba(245,197,24,0.4);
      color:white;
      border-radius:8px;
    }

    .form-control:focus {
      border-color:#f5c518;
      box-shadow:0 0 10px rgba(245,197,24,0.4);
      background:rgba(0,0,0,0.75);
      color:white;
    }

    .btn-cinema {
      background:linear-gradient(45deg,#ff004d,#a700ff);
      color:white;
      font-weight:bold;
      padding:13px;
      border:none;
      border-radius:8px;
      transition:.35s;
      box-shadow:0 0 15px rgba(255,0,77,0.3);
    }

    .btn-cinema:hover {
      transform:scale(1.05);
      box-shadow:0 0 30px rgba(245,197,24,0.5);
    }

    .contact-info {
      font-size:.95rem;
      color:#ccc;
      text-align:center;
      margin-bottom:1.5rem;
    }
  </style>
</head>

<body>
<%- include('partials/header') %>

<main class="container mt-5 mb-5">
  <div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">

      <div class="card cinema-card p-5">

        <h1>Contactez Cinéphoria</h1>

        <p class="contact-info">
          Une question ? Un problème ? Une suggestion ?  
          L’équipe Cinéphoria vous répond dans les plus brefs délais.
        </p>

        <% if (errorMessage) { %>
          <div class="alert alert-danger text-center"><%= errorMessage %></div>
        <% } %>

        <% if (successMessage) { %>
          <div class="alert alert-success text-center">
            <i class="fa fa-check-circle me-2"></i><%= successMessage %>
          </div>
        <% } %>

        <form method="POST" action="/contact">

          <div class="mb-3">
            <label class="cinema-label">Nom (facultatif)</label>
            <input type="text" class="form-control" name="name" value="<%= formData.name || '' %>">
          </div>

          <div class="mb-3">
            <label class="cinema-label">Adresse e-mail *</label>
            <input type="email" class="form-control" name="email" required value="<%= formData.email || '' %>">
          </div>

          <div class="mb-3">
            <label class="cinema-label">Sujet *</label>
            <input type="text" class="form-control" name="title" required value="<%= formData.title || '' %>">
          </div>

          <div class="mb-4">
            <label class="cinema-label">Message *</label>
            <textarea rows="5" class="form-control" name="message" required><%= formData.message || '' %></textarea>
          </div>

          <div class="d-grid">
            <button class="btn btn-cinema">
              <i class="fa fa-paper-plane me-2"></i>Envoyer le message
            </button>
          </div>

        </form>

      </div>
    </div>
  </div>
</main>

<%- include('partials/footer') %>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script> window.addEventListener("load", () => document.body.classList.add("loaded")); </script>
</body>
</html>
