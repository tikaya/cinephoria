<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Gestion des films - Cinéphoria</title>

  <!-- Fonts + Icons + Bootstrap -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&family=Cinzel:wght@600;700&display=swap" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" type="image/png" href="/images/favicon.png">

  <style>
    :root{ 
      --gold:#f5c518; 
      --gold-glow: rgba(245,197,24,0.4);
      --bg1:#0a0f1f; 
      --bg2:#121829; 
      --ink:#eaeaea;
      --purple:#a700ff;
      --pink:#ff004d;
    }
    html, body { height:100%; margin:0; padding:0; }
    
    body{
      display:flex; flex-direction:column; min-height:100vh;
      background: radial-gradient(ellipse at top, #1a2341, #0a0f1f 50%, #000000);
      background-size: 100% 200%;
      animation: gradientFlow 20s ease infinite;
      color: var(--ink);
      font-family: 'Poppins', sans-serif;
      opacity: 0; 
      transition: opacity 1.5s ease;
      position: relative;
      overflow-x: hidden;
    }
    
    /* Particules flottantes */
    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: 
        radial-gradient(2px 2px at 20% 30%, rgba(245,197,24,0.15), transparent),
        radial-gradient(2px 2px at 60% 70%, rgba(167,0,255,0.1), transparent),
        radial-gradient(1px 1px at 50% 50%, rgba(255,0,77,0.1), transparent),
        radial-gradient(1px 1px at 80% 10%, rgba(245,197,24,0.1), transparent),
        radial-gradient(2px 2px at 90% 60%, rgba(167,0,255,0.08), transparent);
      background-size: 200% 200%;
      background-position: 0% 0%;
      animation: particlesFloat 30s ease infinite;
      pointer-events: none;
      z-index: 0;
    }
    
    @keyframes particlesFloat {
      0%, 100% { background-position: 0% 0%; }
      50% { background-position: 100% 100%; }
    }
    
    body.loaded{ opacity: 1; }
    
    @keyframes gradientFlow{ 
      0%{background-position: 50% 0%} 
      50%{background-position: 50% 100%} 
      100%{background-position: 50% 0%} 
    }

    header, footer{
      width:100%;
      background: rgba(10, 15, 31, 0.95);
      backdrop-filter: blur(20px);
      text-align:center; padding: 18px 0;
      color: var(--ink); font-size: 1rem;
      box-shadow: 0 4px 30px rgba(0,0,0,0.7);
      position: relative;
      z-index: 10;
    }
    
    footer{ 
      margin-top:auto;
      border-top: 1px solid rgba(245,197,24,0.15);
    }
    
    main{ 
      flex:1; 
      padding: 0 0 40px; 
      position: relative;
      z-index: 1;
    }

    h1{
      font-family: 'Cinzel', serif;
      font-size: 3rem;
      font-weight: 700;
      background: linear-gradient(135deg, #f5c518 0%, #ffdd57 50%, #f5c518 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      filter: drop-shadow(0 0 20px var(--gold-glow)) drop-shadow(0 0 40px rgba(245,197,24,0.2));
      text-align:center; 
      margin: 50px auto 15px;
      animation: fadeInDown 1.2s ease, titleGlow 3s ease-in-out infinite;
      letter-spacing: 2px;
    }
    
    @keyframes titleGlow {
      0%, 100% { filter: drop-shadow(0 0 20px var(--gold-glow)) drop-shadow(0 0 40px rgba(245,197,24,0.2)); }
      50% { filter: drop-shadow(0 0 30px var(--gold-glow)) drop-shadow(0 0 60px rgba(245,197,24,0.4)); }
    }
    
    .subhero{
      max-width: 1000px; 
      margin: 0 auto 50px; 
      padding: 18px 24px;
      background: linear-gradient(135deg, rgba(255,255,255,0.08) 0%, rgba(255,255,255,0.04) 100%);
      border: 2px solid rgba(245,197,24,0.3);
      border-radius: 20px; 
      backdrop-filter: blur(15px);
      box-shadow: 0 8px 32px rgba(0,0,0,0.6), inset 0 1px 0 rgba(255,255,255,0.1);
      color: #e8ecf8; 
      text-align:center; 
      animation: fadeInUp 1s ease;
      font-size: 1.05rem;
      position: relative;
      overflow: hidden;
    }
    
    .subhero::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: linear-gradient(45deg, transparent, rgba(245,197,24,0.05), transparent);
      animation: shimmer 3s infinite;
    }
    
    @keyframes shimmer {
      0% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
      100% { transform: translateX(100%) translateY(100%) rotate(45deg); }
    }
    
    @keyframes fadeInDown{ from{opacity:0; transform: translateY(-30px)} to{opacity:1; transform:none} }
    @keyframes fadeInUp{ from{opacity:0; transform: translateY(30px)} to{opacity:1; transform:none} }

    /* Cartes Ultra Premium */
    .module-card{
      background: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0.05) 100%);
      border: 2px solid rgba(245,197,24,0.25);
      border-radius: 24px; 
      backdrop-filter: blur(20px);
      box-shadow: 
        0 8px 32px rgba(0,0,0,0.8),
        inset 0 1px 0 rgba(255,255,255,0.1),
        0 0 0 1px rgba(245,197,24,0.1);
      color: #e9edf6;
      transition: all .4s cubic-bezier(0.4, 0, 0.2, 1);
      animation: fadeInUp .9s ease forwards; 
      opacity:0;
      overflow:hidden; 
      height:100%;
      position: relative;
    }
    
    /* Effet de lumière au survol */
    .module-card::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(245,197,24,0.08) 0%, transparent 70%);
      opacity: 0;
      transition: opacity 0.4s ease;
      pointer-events: none;
    }
    
    .module-card:hover::before {
      opacity: 1;
      animation: rotateGlow 4s linear infinite;
    }
    
    @keyframes rotateGlow {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    
    .module-card.visible{ opacity:1; }
    
    .module-card .card-header{
      background: linear-gradient(135deg, rgba(245,197,24,0.15) 0%, rgba(245,197,24,0.08) 100%);
      border-bottom: 2px solid rgba(245,197,24,0.3);
      color: var(--gold); 
      font-weight: 700; 
      font-size: 1.2rem;
      text-shadow: 0 2px 8px rgba(0,0,0,.6);
      padding: 1.2rem;
      letter-spacing: 0.5px;
    }
    
    .module-card .card-body{ 
      padding: 1.5rem; 
      color:#e9edf6 !important;
      position: relative;
      z-index: 2;
    }
    
    .module-card .card-body p{ 
      color:#dde5ff !important; 
      text-shadow:0 1px 4px rgba(0,0,0,.5);
      line-height: 1.6;
    }
    
    .module-card:hover{
      transform: translateY(-12px) scale(1.03);
      box-shadow: 
        0 20px 60px rgba(245,197,24,0.4),
        0 0 80px rgba(245,197,24,0.2),
        inset 0 1px 0 rgba(255,255,255,0.2);
      border-color: rgba(245,197,24,0.6);
    }

    .btn-intra{
      background: linear-gradient(135deg, #ff004d 0%, #c7007a 50%, #a700ff 100%);
      color:#fff; 
      border:none; 
      font-weight:700;
      font-size: 1rem;
      border-radius: 14px; 
      padding: 12px 24px;
      text-shadow: 0 2px 8px rgba(0,0,0,0.8);
      box-shadow: 0 4px 15px rgba(255,0,77,0.4);
      transition: all .3s cubic-bezier(0.4, 0, 0.2, 1);
      position: relative;
      overflow: hidden;
    }
    
    .btn-intra::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
      transition: left 0.5s;
    }
    
    .btn-intra:hover::before {
      left: 100%;
    }
    
    .btn-intra:hover{ 
      transform: scale(1.08) translateY(-2px); 
      box-shadow: 0 8px 30px rgba(255,0,77,0.6), 0 0 40px rgba(167,0,255,0.4);
      background: linear-gradient(135deg, #ff1a5e 0%, #d6008f 50%, #b800ff 100%);
    }

    .hint{ 
      color:#cfd3de; 
      font-size:.98rem; 
      line-height:1.6; 
      margin-bottom: 1.2rem;
    }

    #scrollBtn{
      position: fixed; 
      bottom: 85px; 
      right: 20px;
      width: 56px; 
      height: 56px; 
      border: 2px solid rgba(245,197,24,0.3); 
      border-radius: 50%;
      display: none; 
      cursor: pointer; 
      z-index: 1001;
      color: #fff; 
      background: linear-gradient(135deg, #ff004d, #a700ff);
      box-shadow: 0 4px 20px rgba(245,197,24,0.5), 0 0 40px rgba(255,0,77,0.3);
      transition: all .3s cubic-bezier(0.4, 0, 0.2, 1);
      font-size: 1.2rem;
    }
    
    #scrollBtn:hover{ 
      transform: scale(1.2) rotate(360deg); 
      box-shadow: 0 8px 35px rgba(255,0,77,0.8), 0 0 60px rgba(167,0,255,0.6);
      border-color: rgba(245,197,24,0.6);
    }
    
    /* Animation d'entrée échelonnée */
    .module-card:nth-child(1) { animation-delay: 0.1s; }
    .module-card:nth-child(2) { animation-delay: 0.2s; }
    .module-card:nth-child(3) { animation-delay: 0.3s; }
    
  </style>
</head>
<body>
  <!-- Header intranet -->
  <header>
    <%- include('partials/header_employe', { currentUser }) %>
  </header>

  <main class="container">
    <h1><i class="fa-solid fa-film me-3"></i>Gestion des films</h1>
    <p class="subhero">
      <i class="fa-regular fa-circle-check me-2"></i>
      Créer, modifier et supprimer les films du catalogue.
    </p>

    <!-- Grille : uniquement Créer / Modifier / Supprimer -->
    <div class="row g-5 justify-content-center">
      <!-- Créer -->
      <div class="col-12 col-md-6 col-lg-4">
        <article class="card module-card h-100">
          <div class="card-header">
            <i class="fa-solid fa-circle-plus me-2"></i>Créer un film
          </div>
          <div class="card-body d-flex flex-column">
            <p class="hint">Ajouter un nouveau film (titre, genre, âge minimum, description, affiche).</p>
            <div class="mt-auto">
              <a href="/intranet/films/create" class="btn btn-intra w-100">
                <i class="fa-solid fa-plus me-2"></i> Ouvrir
              </a>
            </div>
          </div>
        </article>
      </div>

      <!-- Modifier -->
      <div class="col-12 col-md-6 col-lg-4">
        <article class="card module-card h-100">
          <div class="card-header">
            <i class="fa-solid fa-pen-to-square me-2"></i>Modifier un film
          </div>
          <div class="card-body d-flex flex-column">
            <p class="hint">Éditer les informations d'un film existant dans la base.</p>
            <div class="mt-auto">
              <a href="/intranet/getAllFilms" class="btn btn-intra w-100">
                <i class="fa-solid fa-pen me-2"></i> Ouvrir
              </a>
            </div>
          </div>
        </article>
      </div>

      <!-- Supprimer -->
      <div class="col-12 col-md-6 col-lg-4">
        <article class="card module-card h-100">
          <div class="card-header">
            <i class="fa-solid fa-trash-can me-2"></i>Supprimer un film
          </div>
          <div class="card-body d-flex flex-column">
            <p class="hint">Retirer un film du catalogue (avec confirmation).</p>
            <div class="mt-auto">
              <a href="/intranet/getAllFilms/delete" class="btn btn-intra w-100">
                <i class="fa-solid fa-trash me-2"></i> Ouvrir
              </a>
            </div>
          </div>
        </article>
      </div>
    </div>
  </main>

  <footer>
    <%- include('partials/footer') %>
  </footer>

  <button id="scrollBtn" aria-label="Retour en haut">
    <i class="fa-solid fa-arrow-up"></i>
  </button>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Apparition progressive des cartes
    document.addEventListener("DOMContentLoaded", () => {
      const cards = document.querySelectorAll(".module-card");
      const observer = new IntersectionObserver(entries => {
        entries.forEach(e => { if (e.isIntersecting) e.target.classList.add("visible"); });
      }, { threshold: 0.12 });
      cards.forEach(c => observer.observe(c));
    });
    
    // Fondu body
    window.addEventListener("load", () => { 
      document.body.classList.add("loaded"); 
    });
    
    // Scroll top
    const scrollBtn = document.getElementById("scrollBtn");
    window.addEventListener("scroll", () => { 
      scrollBtn.style.display = window.scrollY > 260 ? "block" : "none"; 
    });
    
    scrollBtn.addEventListener("click", () => {
      window.scrollTo({ top: 0, behavior: "smooth" });
    });
  </script>
</body>
</html>