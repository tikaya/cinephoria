<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Film créé avec succès - Cinéphoria</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
    }
    body {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      background: radial-gradient(circle at center, #121829, #0a0f1f 85%);
      background-size: 300% 300%;
      animation: gradientMove 18s ease infinite;
      color: #eaeaea;
      font-family: 'Poppins', sans-serif;
      opacity: 0;
      transition: opacity 1.2s ease;
    }
    body.loaded { opacity: 1; }

    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    header, footer {
      width: 100%;
      background: rgba(10, 15, 31, 0.85);
      text-align: center;
      padding: 15px 0;
      color: #eaeaea;
      box-shadow: 0 0 20px rgba(0,0,0,0.6);
    }
    footer { margin-top: auto; }

    main {
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 50px 20px;
      text-align: center;
    }

    h1 {
      font-family: 'Cinzel', serif;
      font-size: 2.6rem;
      color: #f5c518;
      text-shadow: 0 0 15px rgba(245,197,24,0.6);
      animation: fadeInDown 1.2s ease;
      margin-bottom: 30px;
    }

    @keyframes fadeInDown {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .cinema-card {
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(245,197,24,0.4);
      border-radius: 14px;
      backdrop-filter: blur(12px);
      box-shadow: 0 0 25px rgba(0,0,0,0.7);
      color: #fff;
      max-width: 600px;
      width: 100%;
      overflow: hidden;
      opacity: 0;
      transform: translateY(25px);
      animation: fadeInUp 1s ease forwards;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(25px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .cinema-card img {
      width: 100%;
      height: 320px;
      object-fit: cover;
      border-top-left-radius: 14px;
      border-top-right-radius: 14px;
    }

    .cinema-card .card-body {
      padding: 25px;
    }

    .cinema-card h5 {
      color: #f5c518;
      font-size: 1.7rem;
      text-shadow: 0 0 8px rgba(245,197,24,0.5);
    }

    .film-info {
      text-align: left;
      margin-top: 15px;
      font-size: 1rem;
      line-height: 1.6;
    }

    .film-info strong {
      color: #f5c518;
    }

    .badge-heart {
      background: #ff004d;
      color: #fff;
      padding: 6px 12px;
      font-size: 0.85rem;
      border-radius: 10px;
      box-shadow: 0 0 12px rgba(255,0,77,0.7);
      margin-top: 10px;
      display: inline-block;
    }

    .btn-cinema {
      background: linear-gradient(45deg, #ff004d, #a700ff);
      color: #fff;
      border: none;
      font-weight: bold;
      border-radius: 6px;
      padding: 12px 20px;
      margin-top: 25px;
      text-shadow: 0 0 6px rgba(0,0,0,0.8);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .btn-cinema:hover {
      transform: scale(1.05);
      box-shadow: 0 0 25px rgba(245,197,24,0.7);
    }

    .success-icon {
      font-size: 3.2rem;
      color: #28e070;
      margin-bottom: 15px;
      animation: pop 0.8s ease;
    }

    @keyframes pop {
      0% { transform: scale(0.5); opacity: 0; }
      70% { transform: scale(1.2); opacity: 1; }
      100% { transform: scale(1); }
    }

  </style>
</head>
<body>
  <header>
    <%- include('partials/header_employe') %>
  </header>

  <main>
    <i class="fa-solid fa-circle-check success-icon"></i>
    <h1>Film ajouté avec succès !</h1>

    <div class="cinema-card">
      <img src="<%= film.affiche_url || '/images/default_affiche.jpg' %>" alt="Affiche du film <%= film.titre %>">
      <div class="card-body">
        <h5><i class="fas fa-film"></i> <%= film.titre %></h5>

        <div class="film-info mt-3">
          <p><strong>Genre :</strong> <%= film.genre %></p>
          <p><strong>Description :</strong> <%= film.description %></p>
          <p><strong>Âge minimum :</strong> <%= film.age_minimum %> ans</p>
          <% if (film.label_coup_de_coeur) { %>
            <span class="badge-heart">Coup de cœur ❤️</span>
          <% } %>
        </div>

        <div class="text-center">
          <a href="/intranet/films" class="btn btn-cinema mt-4">
            <i class="fas fa-arrow-left"></i> Retour à la gestion des films
          </a>
        </div>
      </div>
    </div>
  </main>

  <footer>
    <%- include('partials/footer') %>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    window.addEventListener("load", () => {
      document.body.classList.add("loaded");
    });
  </script>
</body>
</html>
