<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Mes Réservations - Cinéphoria</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Cinzel:wght@500&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    :root{
      --brand:#f5c518; --bg1:#0e1324; --bg2:#05070f;
      --muted:#a7b0c3; --card:#ffffff0a; --border:#ffffff22;
      --chip:#ffffff10; --chip-border:#ffffff26;
    }
    body{
      background: radial-gradient(circle at 35% 15%, var(--bg1), var(--bg2) 80%);
      color:#eaeaea; font-family:'Poppins',sans-serif;
    }
    header, footer { background: rgba(10,15,31,.88); backdrop-filter: blur(8px); }
    h1{
      font-family:'Cinzel',serif; color:var(--brand);
      text-shadow:0 0 14px rgba(245,197,24,.45);
      margin:26px 0 6px; letter-spacing:.4px;
      font-size: clamp(1.4rem, 2.5vw, 2rem);
    }
    .subtitle{ color:var(--muted); margin-bottom:18px; }

    /* grid */
    .grid{
      display:grid; gap:18px;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      padding-bottom:24px;
    }

    /* card */
    .card-cine{
      background:var(--card); border:1px solid var(--border);
      border-radius:16px; overflow:hidden;
      box-shadow:0 0 28px rgba(0,0,0,.35);
      transition:transform .22s ease, box-shadow .22s ease, border-color .22s ease;
    }
    .card-cine:hover{ transform:translateY(-3px); box-shadow:0 0 44px rgba(245,197,24,.16); border-color:#f5c51855; }
    .poster-wrap{ aspect-ratio:3/4; background:#121522; }
    .poster{ width:100%; height:100%; object-fit:cover; display:block; }

    .state{
      position:absolute; top:10px; left:10px;
      background:rgba(0,0,0,.55); border:1px solid #ffffff33; color:#fff;
      padding:6px 10px; border-radius:999px; font-size:.78rem; display:flex; gap:6px; align-items:center;
    }

    .content{ padding:14px 14px 10px; text-align:left; }
    .title{
      font-family:'Cinzel',serif; color:var(--brand); margin:0 0 8px;
      font-size:1.06rem; line-height:1.25; text-wrap:balance;
    }
    .meta{ display:grid; gap:6px; font-size:.95rem; }
    .meta-row{ display:flex; gap:8px; align-items:center; color:#eaeaea; }
    .meta-row i{ color:var(--brand); width:18px; text-align:center; }

    .chips{ display:flex; flex-wrap:wrap; gap:6px; margin-top:6px; }
    .chip{
      border:1px solid var(--chip-border); background:var(--chip);
      color:#fff; padding:3px 8px; border-radius:999px; font-size:.86rem;
    }

    .price{
      display:flex; align-items:center; gap:8px;
      font-weight:700; color:#fff; margin-top:10px;
    }
    .price .unit{ color:var(--muted); font-weight:500; }

    /* empty state */
    .empty{
      max-width:560px; margin:42px auto; padding:26px; text-align:center;
      background:linear-gradient(180deg,#0b0f1d,#090d19);
      border:1px dashed #ffffff2a; border-radius:16px;
    }
    .empty h2{ font-family:'Cinzel',serif; color:#fff; margin-bottom:8px; font-size:1.2rem; }
    .empty p{ color:var(--muted); margin-bottom:14px; }
    .cta{
      display:inline-flex; gap:8px; align-items:center;
      background:var(--brand); color:#15130f; font-weight:600;
      border-radius:12px; padding:10px 14px; text-decoration:none;
    }
  </style>
</head>
<body class="loaded">
  <header>
    <%- include('../partials/header') %>
  </header>

  <main class="container py-3">
    <h1><i class="fas fa-ticket-alt"></i> Mes Réservations</h1>
    <p class="subtitle">Vos billets, sièges et détails de séance — tout au même endroit.</p>

    <% if (!reservations || reservations.length === 0) { %>
      <section class="empty">
        <h2>Aucune réservation</h2>
        <p>Partez à l’aventure ciné ! Choisissez un film et réservez vos places.</p>
        <a href="/seances" class="cta"><i class="fa-solid fa-film"></i> Voir les séances</a>
      </section>
    <% } else { %>

      <section class="grid">
        <% reservations.forEach(res => { 
             const isPast = new Date(res.date_seance) < new Date();
             const stateLabel = isPast ? 'Terminée' : 'À venir';
             const stateIcon  = isPast ? 'fa-circle-check' : 'fa-hourglass-half';
        %>
          <article class="card-cine">
            <div class="position-relative poster-wrap">
              <img class="poster" loading="lazy"
                   src="<%= res.affiche_url %>"
                   alt="Affiche — <%= res.titre_film %>"
                   onerror="this.onerror=null;this.src='/images/poster-fallback.jpg';this.alt='Affiche indisponible';" />
              <span class="state">
                <i class="fa-solid <%= stateIcon %>" aria-hidden="true"></i> <%= stateLabel %>
              </span>
            </div>

            <div class="content">
              <h2 class="title"><%= res.titre_film %></h2>

              <div class="meta">
                <div class="meta-row"><i class="fa-solid fa-building"></i><span><strong>Cinéma :</strong> <%= res.nom_cinema %></span></div>
                <div class="meta-row"><i class="fa-solid fa-door-open"></i><span><strong>Salle :</strong> <%= res.nom_salle %></span></div>
                <div class="meta-row">
                  <i class="fa-solid fa-calendar-alt"></i>
                  <span><strong>Date :</strong> <%= new Date(res.date_seance).toLocaleDateString('fr-FR', { weekday:'long', year:'numeric', month:'long', day:'numeric' }) %></span>
                </div>
                <div class="meta-row">
                  <i class="fa-solid fa-clock"></i>
                  <span><strong>Heure :</strong> <%= res.heure_debut.substring(0,5).replace(':','h') %> – <%= res.heure_fin.substring(0,5).replace(':','h') %></span>
                </div>
                <div class="meta-row"><i class="fa-solid fa-star"></i><span><strong>Qualité :</strong> <%= res.qualite_projection %></span></div>
                <div class="meta-row"><i class="fa-solid fa-chair"></i><span><strong>Places :</strong> <%= res.nombre_places %></span></div>

                <div class="meta-row"><i class="fa-solid fa-th-list"></i><span><strong>Sièges :</strong></span></div>
                <div class="chips" aria-label="Sièges réservés">
                  <% (res.sieges || []).forEach(s => { %>
                    <span class="chip"><%= s.numero %></span>
                  <% }) %>
                </div>
              </div>

              <div class="price">
                <i class="fa-solid fa-tag" aria-hidden="true"></i>
                <%= parseFloat(res.prix_total).toFixed(2) %><span class="unit"> €</span>
              </div>
            </div>
          </article>
        <% }) %>
      </section>
    <% } %>
  </main>

  <footer>
    <%- include('../partials/footer') %>
  </footer>
</body>
</html>
