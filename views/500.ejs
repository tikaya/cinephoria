<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title><%= title || 'Erreur serveur' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body{background:#0b1121;color:#eaeaea;font-family:system-ui,Segoe UI,Roboto,sans-serif;margin:0;padding:24px}
    .card{background:#11192f;border:1px solid rgba(245,197,24,.18);border-radius:14px;padding:16px;max-width:900px;margin:0 auto}
    pre{white-space:pre-wrap}
    a{color:#f5c518}
  </style>
</head>
<body>
  <div class="card">
    <h1>Erreur serveur</h1>
    <% if (typeof error !== 'undefined') { %>
      <pre><%= error %></pre>
    <% } else { %>
      <p>Une erreur est survenue.</p>
    <% } %>
    <p><a href="/admin">← Retour au dashboard</a></p>
  </div>
  <script>window.addEventListener('load', () => document.body.classList.add('loaded'));</script>
  
</body>
</html>
