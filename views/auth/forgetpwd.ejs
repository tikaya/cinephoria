<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mot de passe oublié - Cinéphoria</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500&family=Cinzel:wght@600&display=swap" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">

  <style>
    /* Forcer lisibilité globale */
p, label, span, small, a, li {
  color: #f2f2f2 !important;
  opacity: 1 !important;
}

/* Corriger les textes Bootstrap "muted" */
.text-muted {
  color: #cccccc !important;
  opacity: 1 !important;
}

/* Texte secondaire plus clair */
p {
  font-size: 1rem;
  line-height: 1.6;
}

/* Liens */
a {
  text-decoration: none;
  font-weight: 500;
}

a:hover {
  color: #ffd966 !important;
}

/* Placeholder input visible */
input::placeholder {
  color: #bbbbbb;
  opacity: 1;
}

/* Forcer les labels */
label {
  color: #f5c518 !important;
}

/* Focus input propre */
.form-control:focus {
  background-color: rgba(0,0,0,0.7);
  color: #ffffff;
}

    body {
      min-height:100vh;
      background: radial-gradient(circle at 20% 30%, #1a1f36, #0a0f1f);
      background-size:300% 300%;
      animation: gradientMove 15s ease infinite, fadeIn 0.8s ease forwards;
      color:#f2f2f2;
      font-family:'Poppins',sans-serif;
      display:flex;
      flex-direction:column;
    }

    @keyframes fadeIn { from{opacity:0} to{opacity:1} }
    @keyframes gradientMove {
      0%{background-position:0% 50%}
      50%{background-position:100% 50%}
      100%{background-position:0% 50%}
    }

    .cinema-card {
      background:rgba(255,255,255,0.12);
      border:1.2px solid rgba(245,197,24,0.45);
      border-radius:18px;
      backdrop-filter:blur(15px);
      box-shadow:0 0 38px rgba(0,0,0,0.75);
      transition:transform .4s, box-shadow .4s;
    }

    .cinema-card:hover {
      transform:scale(1.02);
      box-shadow:0 0 50px rgba(245,197,24,0.35);
    }

    h1 {
      font-family:'Cinzel',serif;
      color:#f5c518;
      text-shadow:0 0 8px rgba(245,197,24,0.4);
      margin-bottom:1.5rem;
    }

    .cinema-label { color:#f5c518; font-weight:600; }

    .form-control {
      background:rgba(0,0,0,0.6);
      border:1px solid rgba(245,197,24,0.4);
      color:#fff;
    }

    .form-control:focus {
      border-color:#f5c518;
      box-shadow:0 0 10px rgba(245,197,24,0.3);
    }

    .btn-cinema {
      background:linear-gradient(45deg,#ff004d,#a700ff);
      color:white;
      font-weight:bold;
      border-radius:8px;
      padding:12px;
      border:none;
    }

    .btn-cinema:hover {
      box-shadow:0 0 22px rgba(245,197,24,0.4);
      transform:scale(1.05);
    }
  </style>
</head>

<body>

<%- include('../partials/header') %>

<main class="container mt-5 mb-5">
  <div class="row justify-content-center">
    <div class="col-md-7 col-lg-5">
      
      <div class="card cinema-card p-5">
        <h1 class="text-center">Mot de passe oublié</h1>

        <p class="text-center mb-4">
          Saisissez votre adresse email.  
          Un mot de passe temporaire vous sera envoyé.
        </p>

        <% if (typeof errorMessage !== "undefined" && errorMessage) { %>
          <div class="alert alert-danger text-center"><%= errorMessage %></div>
        <% } %>

        <form method="POST" action="/forgot-password">


          <div class="mb-3">
            <label class="cinema-label">Adresse e-mail</label>
            <input type="email" name="email" class="form-control" required>
          </div>

          <div class="d-grid">
            <button class="btn btn-cinema">
              <i class="fa fa-envelope"></i> Envoyer
            </button>
          </div>

        </form>

        <div class="text-center mt-4">
          <a href="/login" class="text-warning">Retour à la connexion</a>
        </div>

      </div>

    </div>
  </div>
</main>

<%- include('../partials/footer') %>

</body>
</html>
